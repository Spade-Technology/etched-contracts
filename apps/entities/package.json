{
  "name": "entities",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ entities",
    "create-local": "graph create --node http://localhost:8020/ entities",
    "remove-local": "graph remove --node http://localhost:8020/ entities",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 entities",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.56.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "@graphql-eslint/eslint-plugin": "^3.20.1",
    "matchstick-as": "0.5.0"
  }
}
